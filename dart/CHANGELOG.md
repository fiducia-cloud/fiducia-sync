## 0.2.0

- Add a transactional SQLite sync store for Flutter.
- Add optimistic writes, durable retries, cursor-based pull, and conflict handling.
- Add Supabase realtime/RPC and `fiducia-clients` JSON transport adapters.
