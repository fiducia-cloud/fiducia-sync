# .github

GitHub repo automation for fiducia-sync.

- `workflows/` — CI pipeline (see its README).
- `dependabot.yml` — weekly dependency-update PRs for the SDK's npm packages
  (under `/sdk`) and the GitHub Actions used in CI.
