{
  "name": "@fiducia/sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first sync SDK for fiducia.cloud — an optimistic IndexedDB store reconciled with Postgres over Supabase realtime + the backend WS, powered by the fiducia-sync-core WASM.",
  "exports": {
    ".": "./src/index.mjs",
    "./store": "./src/store.mjs",
    "./htmx": "./src/htmx.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:wasm": "cd .. && wasm-pack build --target bundler --out-dir pkg -- --features wasm",
    "build:wasm-web": "cd .. && wasm-pack build --target web --out-dir pkg-web -- --features wasm",
    "build:admin-bundle": "npm run build:wasm-web && esbuild admin/entry.mjs --bundle --format=iife --target=es2020 --loader:.wasm=binary --outfile=admin/dist/fiducia-sync-admin.js && cp admin/dist/fiducia-sync-admin.js ../../fiducia-admin.rs/assets/fiducia-sync.js",
    "lint:ores": "if [ -f ../../../../.ores-lint/lint.sh ]; then sh ../../../../.ores-lint/lint.sh; else echo 'ores-lint not installed'; fi",
    "prepublishOnly": "npm run lint:ores"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "fake-indexeddb": "^6.0.0"
  },
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  }
}
