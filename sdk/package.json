{
  "name": "@fiducia/sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first sync SDK for fiducia.cloud — an optimistic IndexedDB store reconciled with Postgres over Supabase realtime + the backend WS, powered by the fiducia-sync-core WASM.",
  "exports": {
    ".": "./src/index.mjs",
    "./store": "./src/store.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:wasm": "cd .. && wasm-pack build --target bundler --out-dir pkg -- --features wasm"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
